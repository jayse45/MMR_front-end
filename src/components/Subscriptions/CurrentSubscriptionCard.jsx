import { Card, Typography } from '@mui/material';

const CurrentSubscriptionCard = () => {
	return (
		<Card>
			<Typography>Your current subscription</Typography>
		</Card>
	);
}

export default CurrentSubscriptionCard;
